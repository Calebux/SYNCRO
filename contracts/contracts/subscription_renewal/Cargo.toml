[package]
name = "subscription_renewal"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
subscription_logging = { path = "../subscription_logging" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
proptest = { workspace = true }
subscription_logging = { path = "../subscription_logging" }
