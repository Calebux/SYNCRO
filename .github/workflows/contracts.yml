name: Contracts (Deprecated)
on:
  workflow_dispatch

permissions:
  contents: read

concurrency:
  group: contracts-$ {{ github.workflow }}-$ {{ github.ref }}
  cancel-in-progress: true

jobs:
  build-contracts:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Contracts Deprecated"